# Copy this file to .env.local and fill in your values
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Used by server-side service client (never expose to browser)
SUPABASE_SERVICE_ROLE=
# Optional, used in some links/env-aware logic
NEXT_PUBLIC_SITE_URL=http://localhost:3000
## DEV ONLY: set to true to treat any authenticated user as admin (do not use in production)
ALLOW_ANY_AUTH_AS_ADMIN=false
## Comma-separated admin emails allowed as admins without DB row
NEXT_ADMIN_EMAILS=
